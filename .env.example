# Anthropic API key (sk-ant-api03-*)
# Get it from: console.anthropic.com → API Keys
ANTHROPIC_API_KEY=

# User to run the server as (default: t3user)
# The user is created automatically if it does not exist
RUN_USER=t3user

# Set to true to run as root instead (overrides RUN_USER)
RUN_AS_ROOT=false

# Comma-separated list of domains to block inside the container (no spaces)
# Routed to 0.0.0.0 via /etc/hosts — connection fails immediately
# Example: BLOCKED_DOMAINS=facebook.com,reddit.com,twitter.com
BLOCKED_DOMAINS=
